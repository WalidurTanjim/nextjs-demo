const DefaultLoadingPage = () => {
    return (
        <section className={`default-loading-page w-full py-20 text-center`}>
            <h1 className={`text-2xl md:text-3xl lg:text-4xl font-semibold text-slate-700`}>Loading...</h1>
        </section>
    );
};

export default DefaultLoadingPage;